---
# Python Django Role - Default Variables

app_user: "app"
app_dir: "/opt/app"
backend_dir: "{{ app_dir }}/backend"

python_version: "3.10"
django_settings_module: "copilot_orchestrator.settings"

# Services to manage
services:
  - coco-daphne
  - coco-celery-worker
  - coco-celery-beat
