"""
Production Settings Override for Copilot Orchestrator
Generated by Ansible deployment

This file extends the base settings.py with production-specific configurations.
Usage: DJANGO_SETTINGS_MODULE=copilot_orchestrator.settings_prod
"""
import os
from pathlib import Path

# Import all base settings
from copilot_orchestrator.settings import *

# Override/add production settings that base settings.py doesn't have

# Static files configuration (required for collectstatic)
STATIC_ROOT = os.getenv('STATIC_ROOT', '{{ backend_dir }}/staticfiles')
STATIC_URL = os.getenv('STATIC_URL', '/static/')

# Media files configuration
MEDIA_ROOT = os.getenv('MEDIA_ROOT', '{{ backend_dir }}/media')
MEDIA_URL = os.getenv('MEDIA_URL', '/media/')

# Ensure DEBUG is False in production
DEBUG = False

# Security settings for production
SECURE_SSL_REDIRECT = False  # Nginx handles SSL termination
SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')

# NOTE: SESSION_COOKIE_SECURE and CSRF_COOKIE_SECURE are configured in .env.prod
# and loaded by base settings.py. Do NOT override them here.
# For CloudFront deployments, they must be True to support SameSite=None cookies.
