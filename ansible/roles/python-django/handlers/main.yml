---
# Python Django Role - Handlers

- name: reload systemd
  systemd:
    daemon_reload: yes

- name: restart services
  systemd:
    name: "{{ item }}"
    state: restarted
  loop:
    - coco-daphne
    - coco-celery-worker
    - coco-celery-beat
